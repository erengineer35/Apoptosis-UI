# GitHub Pages Frontend

This folder is the static frontend for GitHub Pages.

It does not run Python, PyTorch, or the model. Use the Backend URL field to point the interface to the running FastAPI backend.
